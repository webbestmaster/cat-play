import BaseModel from './../../../core/Base-model';
import CONST from './tic-tac-toe-const';

export default class PlayerModel extends BaseModel {

    /**
     *
     * @param args - {mind: 'human' or 'CPU', weapon: 'X' or 'O'}
     */

    constructor(args) {
        super(args);
    }


}